name: 'nasdaq_volume'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  nasdaq_volume:
    staging:
      #+schema: staging
      +materialized: view
    dimensions:
      #+schema: dimensions
      +materialized: table
    facts:
      #+schema: facts
      +materialized: table
    marts:
      #+schema: marts
      +materialized: view